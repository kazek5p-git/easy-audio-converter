# Easy Audio Converter 1.9.2

This maintenance release fixes the Spanish translation of the `Disabled` state
and replaces the bundled legacy UPX-packed GOGO-no-coda executable with a
functionally identical unpacked binary. The change is intended to reduce
heuristic antivirus false positives while preserving the optional GOGO WAV to
MP3 conversion path.
